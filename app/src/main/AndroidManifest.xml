<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Launcher capabilities.
      QUERY_ALL_PACKAGES: Required for launchers to list installed apps (legitimate use case).
      SET_WALLPAPER: To change the wallpaper from within the launcher.
      EXPAND_STATUS_BAR: Optional gesture to open the notification shade.
      BIND_APPWIDGET: Required to host app widgets on the home screen.
      REQUEST_DELETE_PACKAGES: To show the system uninstall dialog for apps.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />
    <uses-permission
        android:name="android.permission.BIND_APPWIDGET"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <application
        android:name=".LauncherApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PixelLikeLauncher"
        tools:targetApi="34">

        <!--
          Main launcher activity.
          - HOME + DEFAULT: This activity can be assigned as the device's default home app.
          - launchMode=singleTask: Reusing the same task when HOME is pressed repeatedly.
          - stateNotNeeded=true: Home activity does not need state restoration across restarts.
          - configChanges: Handle rotation/size changes in-place to preserve scroll state (no recreation).
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="adjustPan"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:theme="@style/Theme.PixelLikeLauncher">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>
</manifest>
